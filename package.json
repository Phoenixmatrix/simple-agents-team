{
  "name": "simple-agents-team",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "typecheck": "tsgo --build",
    "lint": "oxlint packages/",
    "check": "tsgo --build && oxlint packages/",
    "sat": "bun packages/sat/src/index.ts",
    "tracker": "bun packages/tracker/src/index.ts",
    "workers": "bun packages/workers/src/index.ts",
    "get-agent-name": "bun packages/sat/src/get-agent-name.ts",
    "spawn-worker": "bun packages/sat/src/spawn-worker.ts",
    "daemon": "bun packages/daemon/src/index.ts"
  },
  "devDependencies": {
    "@typescript/native-preview": "^7.0.0-dev.20260325.1",
    "bun-types": "^1.3.11",
    "oxlint": "^1.57.0"
  }
}
