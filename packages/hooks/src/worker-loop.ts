export {};

console.log(JSON.stringify({
  userMessage: "Wake up and initialize. Check your name with echo $SAT_AGENT_NAME, then run bun tracker tasks to find tasks assigned to you. If there is a task assigned to you, run bun tracker tasks start <id> and work on it. When done, run bun tracker tasks done <id>.",
}));
